[pytest]
addopts = -q --reruns=2
testpaths = tests
markers =
    smoke: quick smoke tests
    regression: full regression

# Note: --reruns=2 causes pytest-rerunfailures to retry failed tests up to 2 times.
